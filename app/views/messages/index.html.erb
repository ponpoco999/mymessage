<h1>投稿一覧</h1>
<% @messages.each do |message| %>
  <%= render partial: "message", locals: { message: message } %>
<% end %>
<br>
<a href="/messages/new" class="btn btn-primary">新規投稿</a>
<%= paginate(@messages) %>

